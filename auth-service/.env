PORT=3001
# Shared secret — MUST be identical in api-gateway so it can verify tokens.
JWT_SECRET=super-secret-erp-demo-key-change-me
JWT_EXPIRES_IN=3600s
# Where to reach the user-service for credential checks
USER_SERVICE_URL=http://localhost:3002
